1) What is the difference between null and undefined?
ans: null is empty value and undefined is javascript's default value

2) What is the use of the map() function in JavaScript? How is it different from forEach()?
ans: map is used for looping in javascript.map gives us output in console but forEach() doesn't give the output.

3) What is the difference between == and ===?
ans: == means it checks only values and === means it checks data types and values.

4) What is the significance of async/await in fetching API data?
ans: async await makes the function asyncronous which helps to make the code easily readable and it reduce the number of promise chaining in the code.

5) Explain the concept of Scope in JavaScript (Global, Function, Block).
ans: global scope is we can use its value in anywhere.In function scope it's value is usable just inside the function.block scope is tha it's value is usable just inside the block which is any function and loops.